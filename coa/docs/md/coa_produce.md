## coa produce

Start a system remastering production flight

### Synopsis

The 'produce' command is the core of the remastering process. 
It orchestrates the creation of a bootable live ISO using OverlayFS for a zero-copy approach.

Supported modes:
  - standard: Creates a fresh live system (purging host user identities).
  - clone: Preserves the host's /home directory and user identities.
  - crypted: Encapsulates the root filesystem inside a LUKS2 container.

```
coa produce [flags]
```

### Examples

```
  # Start a standard ISO production in the default /home/eggs nest
  sudo coa produce

  # Start a clone production in a custom path
  sudo coa produce --mode clone --path /mnt/storage/workspace

  # Start an encrypted live production
  sudo coa produce --mode crypted
```

### Options

```
  -h, --help          help for produce
      --mode string   standard, clone, or crypted (default "standard")
      --path string   working directory (default "/home/eggs")
```

### SEE ALSO

* [coa](coa.md)	 - coa (brooding in my dialect) - The Mind orchestrator

###### Auto generated by spf13/cobra on 12-Apr-2026
